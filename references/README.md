# References

Binary reference files were intentionally omitted from this public bundle.

Reason:
- keep the public repo text-auditable
- avoid publishing opaque binaries by default
- reduce accidental exposure risk in permanent Git history

Public-facing narrative and specification content has been preserved in the markdown docs and specs directories.
If a binary packet is needed later, publish it separately after a dedicated content review.
